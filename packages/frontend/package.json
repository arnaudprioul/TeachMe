{
  "name": "@teachme/frontend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 5000",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt preview --port 5000",
    "clean": "node -e \"['dist','.nuxt','.output'].forEach(d=>require('fs').rmSync(d,{recursive:true,force:true}))\""
  },
  "dependencies": {
    "@nuxtjs/i18n": "~9.5.0",
    "nuxt": "~3.16.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  }
}
